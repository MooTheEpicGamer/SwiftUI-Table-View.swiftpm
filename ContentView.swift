import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "Peter_Griffin")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Ay Peeta")
        }
    }
}
